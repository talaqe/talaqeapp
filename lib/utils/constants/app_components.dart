import 'package:talaqe/utils/constants/app_constants.dart';
import 'package:flutter/material.dart';

/// This file contains various components for the app
class AppComponents {
  static const defaultBorderRadius = Radius.circular(10);
  static const mediumBorderRadius = Radius.circular(15);
  static const largeBorderRadius = Radius.circular(20);
  static const largerBorderRadius = Radius.circular(25);
  static const hugeBorderRadius = Radius.circular(40);
  static final defaultBorder = Border.all(color: AppColors.borderColor);
  static const headerTextStyle =
      TextStyle(fontSize: 18, fontWeight: FontWeight.bold);
}
